进度：mybatis完成至实现多对多查询
自学中没怎么理解深的问题：
    全局配置文件中：
注意点：1、 在SqlMapConfig.xml文件中配置中定义别名是也就是typeAliases标签，它定义所处的位置很重要
       它必须放在Properties标签与environments标签之间，其它位置都会导致爆红出错，以致配置文件读不出。

       2、 在动态SQL的映射文件中，拼接sql语句，在用if判断是否为空时，这null不能大写，不然就会报错，这个报错卡了很一会，因为感觉大写NULL和小
        写null是没区别的，但这里就是只能用小写。

疑惑点：1、 有个package标签，表示注册指定包下的所有的映射文件，但在测试中，发现有的配置文件识别不出来，接口与映射文件名称
        是相同的，并且放到了一个目录下，但有的配置文件读不出报错。

       2、 在一对多和多对多查询实现中，发现查询出来的结果，由外键建立联系的表的主键显示为0或者匹配不对，意思就是这一张表的外键输出正确，但另一张表作为主键却显示不出或者不对
       如下图，在一对多和多对多都出现这个问题。